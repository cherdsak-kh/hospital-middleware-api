# Application configuration
PORT=5000
APP_ENV=development

# Database configuration (Host port 5433 mapped to avoid conflict with existing PostgreSQL)
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgrespassword
DB_NAME=hospital_db
DB_SSLMODE=disable

# JWT configuration
JWT_SECRET=supersecret_jwt_key_agnos_2026
JWT_EXPIRY_HOURS=24

# External HIS integration
HOSPITAL_A_BASE_URL=https://hospital-a.api.co.th
